# Placeholder for additional Python unit tests.
# Main tests are in AutomatedLandmarking.py (AutomatedLandmarkingTest class),
# registered via slicer_add_python_unittest in the parent CMakeLists.
# To add separate test scripts, uncomment and adjust:
# slicer_add_python_unittest(SCRIPT SomeTestScript.py)
